<%= content_for :title, 'Éditer une ferme' %>

<%= render :layout => 'components/header', locals: {title: 'Éditer une ferme'} do %>
  <%= header_link_tag farm_path(@farm), 'Détails', 'eye'  %>
<% end %>

<%= render "form", farm: @farm %>
